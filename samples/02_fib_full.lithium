# Twilight Programming Language

module Fib (

  import print from "twilight/core"

  class Main (

    def fib(x:Number):
      when: (
        x < 3:  1
         else:  fib(x - 1) + fib(x-2)
      )

  )
);
